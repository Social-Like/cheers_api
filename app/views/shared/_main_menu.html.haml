.navbar
  .navbar-inner.bg-header
    .container
      / .btn-navbar is used as the toggle for collapsed navbar content
      %a.btn.btn-navbar{ data: { toggle: "collapse", target: ".nav-collapse" } }
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.brand{ href: root_url }
        = image_tag("logo-cheers.png")
        %strong        
          Panel de control

      %ul.nav.pull-right.pt20
        %li.dropdown
          %a.dropdown-toggle{ data: {toggle: "dropdown"}, href: "#" }
            %strong
              = current_user.full_name
              %b.caret
          %ul.dropdown-menu
            %li
              = link_to t("labels.logout"), destroy_user_session_url, method: :delete
